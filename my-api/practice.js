// string
const userName = "renato";

// number
const userAge = 20;

// float
const accountMoney = 20.22;

// array
const myFruits = ["banana", "mango", "apple"];

// for
myFavoriteFruits = myFruits.filter((fruit) => {
  return fruit !== "mango";
});

// date
userBirthday = new Date();

// random
const diceRoll = Math.floor(Math.random() * 6) + 1;

// foreach
myFruits.forEach((fruit) => {
  console.log("fruit", fruit);
});
